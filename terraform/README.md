# devops-netology/terraform
»гнорируютс€:
локальные .terraform директории;
файлы .tfstate;
файлы журнала сбоев;
все файлы .tfvars, которые могут содержать важные данные, такие как пароль, частные ключи и другие секреты; они не должны быть частью контрол€ версий, так как представл€ют собой точки данных, которые потенциально чувствительны и подвержены изменению в зависимости от среды;
файлы переопределени€, поскольку они обычно используютс€ дл€ локального переопределени€ ресурсов и поэтому не провер€ютс€;
- включить файлы переопределени€, которые хотим добавить в систему контрол€ версий - использу€ шаблон с отрицанием (! ѕример_override.tf);
- включить файлы tfplan, чтобы игнорировать вывод плана команды: terraform plan -out = tfplan (пример: * tfplan *);
файлы конфигурации CLI.